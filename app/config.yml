title: Site title
description: >- # this means to ignore newlines until "baseurl:"

# Build settings
plugins:
  - jekyll-feed

collections_dir: index
emptyArray: []

collections:
  entries:
    output: true

include:
  - _redirects

# variables
edit_url: 'https://github.com/scientistkev/philanthrobotics-grants-index' # edit url goes here
displayed_fields: "associated_platform, point_of_contact, notes, fields_included, cost_to_access" # displayed fields go here (leave out title, location, tags -- )
background_color: 'aliceblue'